import React from 'react';

export const Filler = () => {
  return (
    <p className="BANKING APPLICATION">
      This is a banking application. It simply can be used to open account and transaction. Our main focus has to use different cloud tools.
    </p>
  );
};

export default Filler;
